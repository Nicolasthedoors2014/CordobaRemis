<!DOCTYPE html>
<html lang="en">
  <head>
    <title>CordobaRemis</title>
    <%= csrf_meta_tags %>
    <link href="https://fonts.googleapis.com/css?family=Ubuntu" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="body">

    <header class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top ">
      <%= link_to 'CordobaRemis', root_path, class: 'navbar-brand' %>

      <% if passenger_signed_in? %>
      <div class="navbar-nav mr-auto text-center">
        <li class="nav-item nav-link"><%= "Hola " + current_passenger.name + "!" %></li>
         <%= link_to('Pedir viaje',trip_new_url, class: 'nav-item nav-link') %>

        <li class="nav-item dropdown">
       <a class="nav-link dropdown-toggle " href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Ayuda
       </a>
       <div class="dropdown-menu " aria-labelledby="navbarDropdownMenuLink">
         <a class="dropdown-item" href="#">Quienes Somos</a>
         <a class="dropdown-item" href="#"> Como funciona</a>
         <a class="dropdown-item " href="#"> Preguntas frecuentes</a>
       </div>
     </li>
      </div>

      <div class="navbar-nav nav-cor">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle " href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Mi Perfil
          </a>
          <div class="dropdown-menu " aria-labelledby="navbarDropdownMenuLink">
            <%= link_to('Ver',passenger_url(current_passenger.id), class: 'dropdown-item') %>
            <%= link_to('Editar',edit_passenger_registration_path, class: 'dropdown-item') %>
            <%= link_to('Cerrar sesión', destroy_passenger_session_path,class: 'dropdown-item', :method => :delete) %>
          </div>
        </li>
      </div>

      <% elsif driver_signed_in? %>
      <div class="navbar-nav mr-auto text-center">
        <li class="nav-item nav-link"><%= "Hola " + current_driver.name + "!" %></li>
        <li class="nav-item dropdown">
       <a class="nav-link dropdown-toggle " href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Ayuda
       </a>
       <div class="dropdown-menu " aria-labelledby="navbarDropdownMenuLink">
         <a class="dropdown-item" href="#">Quienes Somos</a>
         <a class="dropdown-item" href="#"> Como funciona</a>
         <a class="dropdown-item " href="#"> Preguntas frecuentes</a>
       </div>
     </li>
      </div>

      <div class="navbar-nav nav-cor">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle " href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Mi Perfil
          </a>
          <div class="dropdown-menu " aria-labelledby="navbarDropdownMenuLink">
            <%= link_to('Ver', driver_url(current_driver.id), class: 'dropdown-item') %>
            <%= link_to('Editar',edit_driver_registration_path, class: 'dropdown-item') %>
            <%= link_to('Cerrar sesión', destroy_driver_session_path,class: 'dropdown-item', :method => :delete) %>
          </div>
        </li>
      </div>

      <% else %>
      <div class="navbar-nav mr-auto text-center">
        <a class="nav-item nav-link " href="#">Quienes Somos</a>
        <a class="nav-item nav-link" href="#">Como Funciona</a>
        <%= link_to('Registrarse', session_helper_register_path, class: 'nav-item nav-link') %>
        <%= link_to('Iniciar sesión', session_helper_login_path, class: 'nav-item nav-link') %>
      </div>
      <%end%>

    </header>
      <%= yield %>
  </body>
</html>
